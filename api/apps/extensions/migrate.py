# from flask_migrate import Migrate


# TODO - Authenticate users against SQL Alchemy User Model and return JWT.

# class SQLMigrate(object):
#
#     def __init__(self):
#         self.migrate = Migrate
#
#     def init_app(self, app, sqldb):
#         return self.migrate(app, sqldb, directory=app.config['MIGRATE_DIR'])
#
# migrate = SQLMigrate()
